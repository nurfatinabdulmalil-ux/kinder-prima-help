# Kinder Prima Help Android app

The native Android shell loads its offline site from `app/src/main/assets/index.html`.

Add later pages in that same folder (for example `lesson-two.html`) and link them with relative URLs. Install Android Studio, open `android-app`, then choose **Build > Build APK(s)**. The app requires no Internet permission.
